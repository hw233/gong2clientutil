package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 活动子类表.csv
 * 活动子类id = id
 * 子类类型 = childType
 * 关联任务 = childSubitemId
 */
public class WeekActivityChildData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        init1();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    1, map("id", 1, "childType", 1, "childSubitemId", list(200001)),
    2, map("id", 2, "childType", 1, "childSubitemId", list(200002)),
    3, map("id", 3, "childType", 1, "childSubitemId", list(200003)),
    4, map("id", 4, "childType", 1, "childSubitemId", list(200004)),
    5, map("id", 5, "childType", 1, "childSubitemId", list(200005))
);
    }

    private static void init1() {
data.map(
    6, map("id", 6, "childType", 1, "childSubitemId", list(200006)),
    7, map("id", 7, "childType", 1, "childSubitemId", list(200007))
);
    }

}