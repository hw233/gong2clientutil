package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 七天开服活动表.csv
 * 天数 = id
 * 活动子类 = activityChild
 */
public class WeekActivityData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        init1();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    1, map("id", 1, "activityChild", list(1)),
    2, map("id", 2, "activityChild", list(2)),
    3, map("id", 3, "activityChild", list(3)),
    4, map("id", 4, "activityChild", list(4)),
    5, map("id", 5, "activityChild", list(5))
);
    }

    private static void init1() {
data.map(
    6, map("id", 6, "activityChild", list(6)),
    7, map("id", 7, "activityChild", list(7))
);
    }

}