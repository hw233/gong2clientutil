package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 机器人表.csv
 * 机器人编号 = id
 * 机器人名称 = name
 * 机器人性别 = sex
 * 机器人等级 = lv
 * 适用范围 = putCon
 * 阵法 = zhenfa
 * uid = uid
 * 阵法等级 = zhenfaLv
 * 阵位 = position
 * 门徒选择 = mentu
 * 图标 = iconIndex
 * VIP = vipLv
 * 版本号 = version
 */
public class RobotData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    99999, map("id", 99999, "name", "果动小精灵", "sex", 1, "lv", 100, "putCon", 3, "zhenfa", 31000, "uid", 99999, "zhenfaLv", list(1, 1), "position", list(1, 1, 1, 1, 1, 1), "mentu", 10001, "iconIndex", 40004, "vipLv", 15, "version", 41)
);
    }

}