package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 剧情特效表.csv
 * 编号 = id
 * 特效内容描述 = specialeffects
 */
public class SpecialeffectsData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        init1();
        init2();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    10000, map("id", 10000, "specialeffects", "手机微微振动"),
    10001, map("id", 10001, "specialeffects", "屏幕场景抖动"),
    10002, map("id", 10002, "specialeffects", "场景定义表id为10，主角立绘显示在屏幕中央。"),
    10003, map("id", 10003, "specialeffects", "在画面上加一块灰色带有透明度的遮挡，使屏幕整体变灰"),
    10004, map("id", 10004, "specialeffects", "在当前锚点下，屏幕小幅度左右移动")
);
    }

    private static void init1() {
data.map(
    10005, map("id", 10005, "specialeffects", "刀光剑影特效，与该句对话同时出现"),
    10006, map("id", 10006, "specialeffects", "屏幕下方出现灰色的雾并伴有粒子特效。"),
    10007, map("id", 10007, "specialeffects", "人物立绘向下移动，减淡并消失"),
    10008, map("id", 10008, "specialeffects", "人物立绘向上移动，透明度渐渐变高"),
    10009, map("id", 10009, "specialeffects", "屏幕中间出现一个信封图标")
);
    }

    private static void init2() {
data.map(
    10010, map("id", 10010, "specialeffects", "进入战斗场景后先进行剧情。")
);
    }

}