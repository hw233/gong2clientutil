package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 场景npc表.csv
 * 场景编号 = id
 * 场景名称 = scenename
 * npcid = npcid
 * npc坐标 = npczuobiao
 */
public class SceneNpcData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    1001, map("id", 1001, "scenename", "南府老宅", "npcid", list(5006, 5008, 5015, 40021), "npczuobiao", list(list(1, 1), list(2, 2), list(3, 3), list(4, 4)))
);
    }

}