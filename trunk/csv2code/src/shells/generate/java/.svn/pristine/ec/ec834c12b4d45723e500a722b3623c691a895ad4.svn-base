package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 悬赏难度定义表.csv
 * 描述 = describe
 * 刷新权重 = props
 * 消耗体力值 = cost
 */
public class WantedDifficultyDate extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    1, map("id", 1, "describe", "简单", "props", 5000, "cost", 10),
    2, map("id", 2, "describe", "普通", "props", 3000, "cost", 20),
    3, map("id", 3, "describe", "困难", "props", 2000, "cost", 30)
);
    }

}