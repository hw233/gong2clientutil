package com.gamejelly.gong2.config.data;

import com.gamejelly.gong2.config.data.base.LData;
import com.gamejelly.gong2.config.data.base.LMap;

/**
 * 派系活跃度表.csv
 * 奖励编号 = id
 * 激活活跃度 = number
 * 奖励 = reward
 */
public class FactionsVitalityData extends LData {
    public static final LMap data = new LMap();

    static {
        init0();
        data.setFrozen(true);
    }

    private static void init0() {
data.map(
    1, map("id", 1, "number", 500, "reward", list(44)),
    2, map("id", 2, "number", 1000, "reward", list(45)),
    3, map("id", 3, "number", 2000, "reward", list(46)),
    4, map("id", 4, "number", 4000, "reward", list(47)),
    5, map("id", 5, "number", 6000, "reward", list(48))
);
    }

}